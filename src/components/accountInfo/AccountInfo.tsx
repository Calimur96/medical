import { FC } from "react";

import "./AccountInfo.scss";

const AccountInfo: FC = () => {
    return (
        <div className="account-info">
            <div className="account-info__inner">
                <div className="account-info__profile"></div>
            </div>
        </div>
    );
};

export default AccountInfo;
